# 🚀 Подробный гайд по деплою на Render

Этот гайд поможет вам выгрузить приложение "Карамба" на платформу Render.

## 📋 Содержание

1. [Подготовка проекта](#подготовка-проекта)
2. [Создание аккаунта на Render](#создание-аккаунта-на-render)
3. [Подготовка репозитория](#подготовка-репозитория)
4. [Создание веб-сервиса на Render](#создание-веб-сервиса-на-render)
5. [Настройка деплоя](#настройка-деплоя)
6. [Проверка работы](#проверка-работы)
7. [Решение проблем](#решение-проблем)

---

## 1. Подготовка проекта

### 1.1. Проверка локальной сборки

Перед деплоем убедитесь, что проект собирается локально:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект
npm run build
```

Если сборка прошла успешно, в папке `dist` появится готовое приложение.

### 1.2. Проверка preview

Проверьте, что собранное приложение работает:

```bash
npm run preview
```

Откройте браузер и проверьте, что все работает корректно.

---

## 2. Создание аккаунта на Render

### 2.1. Регистрация

1. Перейдите на сайт [Render.com](https://render.com)
2. Нажмите кнопку **"Get Started for Free"** или **"Sign Up"**
3. Выберите способ регистрации:
   - **GitHub** (рекомендуется) - самый простой способ
   - **GitLab**
   - **Bitbucket**
   - **Email** - регистрация через email

### 2.2. Подтверждение email

Если регистрировались через email, проверьте почту и подтвердите аккаунт.

---

## 3. Подготовка репозитория

### 3.1. Создание Git репозитория

Если у вас еще нет Git репозитория:

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Карамба язык программирования"
```

### 3.2. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните форму:
   - **Repository name**: `karamba-lang` (или любое другое имя)
   - **Description**: "Язык программирования Карамба с компилятором"
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (если они уже есть)
4. Нажмите **"Create repository"**

### 3.3. Загрузка кода на GitHub

GitHub покажет инструкции. Выполните команды:

```bash
# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/karamba-lang.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

---

## 4. Создание веб-сервиса на Render

### 4.1. Подключение репозитория

1. Войдите в [Render Dashboard](https://dashboard.render.com)
2. Нажмите кнопку **"New +"** в правом верхнем углу
3. Выберите **"Static Site"** (для статических сайтов, включая Vite приложения)

### 4.2. Настройка подключения

1. **Connect a repository**:
   - Если используете GitHub, нажмите **"Connect GitHub"**
   - Разрешите Render доступ к вашим репозиториям
   - Выберите репозиторий `karamba-lang` (или ваш репозиторий)

2. **Configure your static site**:
   - **Name**: `karamba-lang` (или любое другое имя)
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: оставьте пустым (если проект в корне репозитория)
   - **Build Command**: `npm run build`
   - **Publish Directory**: `dist`

### 4.3. Дополнительные настройки (опционально)

- **Environment Variables**: обычно не нужны для этого проекта
- **Headers**: можно добавить позже, если понадобится

### 4.4. Создание сервиса

Нажмите кнопку **"Create Static Site"**

---

## 5. Настройка деплоя

### 5.1. Автоматический деплой

Render автоматически:
1. Клонирует ваш репозиторий
2. Установит зависимости (`npm install`)
3. Выполнит команду сборки (`npm run build`)
4. Опубликует файлы из папки `dist`

### 5.2. Мониторинг процесса

В разделе **"Events"** вы можете видеть:
- Логи установки зависимостей
- Логи сборки
- Ошибки (если есть)

### 5.3. Первый деплой

Первый деплой может занять 5-10 минут. Дождитесь завершения.

---

## 6. Проверка работы

### 6.1. Получение URL

После успешного деплоя Render предоставит вам URL:
- Формат: `https://karamba-lang.onrender.com` (или ваш кастомный домен)
- URL будет показан в верхней части страницы сервиса

### 6.2. Тестирование

1. Откройте URL в браузере
2. Проверьте основные функции:
   - ✅ Открывается приветственная страница
   - ✅ Работает редактор кода
   - ✅ Можно запустить код
   - ✅ Работает учебник
   - ✅ Подсветка синтаксиса работает

### 6.3. Проверка на мобильных устройствах

Откройте сайт на телефоне/планшете, чтобы убедиться, что адаптивность работает.

---

## 7. Решение проблем

### 7.1. Ошибка сборки

**Проблема**: Build failed

**Решение**:
1. Проверьте логи в разделе **"Events"**
2. Убедитесь, что `npm run build` работает локально
3. Проверьте версии Node.js (Render использует последнюю LTS версию)
4. Убедитесь, что все зависимости указаны в `package.json`

### 7.2. Ошибка 404 при переходе по ссылкам

**Проблема**: При обновлении страницы или переходе по прямой ссылке получаете 404

**Решение**: 
Нужно настроить редирект для SPA (Single Page Application).

Создайте файл `public/_redirects` (если папки `public` нет, создайте её):

```
/*    /index.html   200
```

Или создайте файл `render.yaml` в корне проекта:

```yaml
static_sites:
  - name: karamba-lang
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
```

### 7.3. Медленная загрузка

**Проблема**: Сайт загружается медленно

**Решение**:
- Render бесплатный план может "засыпать" после 15 минут бездействия
- Первая загрузка после "сна" может занять 30-60 секунд
- Для продакшена рассмотрите платный план или другой хостинг

### 7.4. Проблемы с путями

**Проблема**: Ресурсы (CSS, JS) не загружаются

**Решение**:
Проверьте `vite.config.ts` - убедитесь, что base path правильный:

```typescript
export default defineConfig({
  plugins: [react()],
  base: '/', // или '/karamba-lang/' если деплоите в подпапку
})
```

### 7.5. Ошибки TypeScript

**Проблема**: TypeScript ошибки при сборке

**Решение**:
1. Проверьте `tsconfig.json`
2. Убедитесь, что все типы корректны
3. Запустите `npm run build` локально перед пушем

---

## 8. Обновление сайта

### 8.1. Автоматическое обновление

Render автоматически деплоит изменения при каждом push в основную ветку:

```bash
# Внесите изменения в код
# ...

# Закоммитьте изменения
git add .
git commit -m "Описание изменений"

# Загрузите на GitHub
git push origin main
```

Render автоматически обнаружит изменения и начнет новый деплой.

### 8.2. Ручной деплой

Если нужно запустить деплой вручную:
1. Откройте ваш сервис в Render Dashboard
2. Перейдите в раздел **"Manual Deploy"**
3. Выберите ветку и нажмите **"Deploy latest commit"**

---

## 9. Дополнительные настройки

### 9.1. Кастомный домен

1. В настройках сервиса найдите раздел **"Custom Domains"**
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### 9.2. Environment Variables

Если понадобятся переменные окружения:
1. Откройте настройки сервиса
2. Перейдите в **"Environment"**
3. Добавьте переменные (Key: Value)

### 9.3. Автоматические деплои из других веток

По умолчанию деплоится только `main` ветка. Чтобы деплоить из других веток:
1. В настройках сервиса измените **"Branch"**
2. Или создайте отдельный сервис для другой ветки

---

## 10. Полезные команды

### Локальная проверка перед деплоем

```bash
# Очистка предыдущей сборки
rm -rf dist

# Установка зависимостей
npm install

# Проверка типов
npm run build

# Локальный preview
npm run preview
```

### Git команды для деплоя

```bash
# Проверка статуса
git status

# Добавление изменений
git add .

# Коммит
git commit -m "Описание изменений"

# Push на GitHub (автоматически запустит деплой на Render)
git push origin main
```

---

## 11. Чек-лист перед деплоем

- [ ] Проект собирается локально (`npm run build`)
- [ ] Preview работает корректно (`npm run preview`)
- [ ] Код загружен на GitHub
- [ ] Репозиторий подключен к Render
- [ ] Настроены правильные команды сборки
- [ ] Указана правильная папка публикации (`dist`)
- [ ] Проверены логи первого деплоя
- [ ] Сайт открывается и работает корректно

---

## 12. Контакты и поддержка

- **Render Documentation**: [https://render.com/docs](https://render.com/docs)
- **Render Support**: [https://render.com/support](https://render.com/support)
- **Vite Documentation**: [https://vitejs.dev](https://vitejs.dev)

---

## 🎉 Готово!

Теперь ваше приложение "Карамба" доступно в интернете! 

**Ваш URL**: `https://your-app-name.onrender.com`

Поделитесь ссылкой с друзьями и коллегами! 🚀

