# 🏴‍☠️ Карамба - Язык программирования

Собственный язык программирования с русскими ключевыми словами, компилятором и графическим редактором кода.

## Возможности

- ✨ Русские ключевые слова для удобства
- 🚀 Полнофункциональный компилятор и интерпретатор
- 💻 Красивый веб-редактор с подсветкой синтаксиса
- 📝 Поддержка переменных, условий, циклов и функций
- 🎨 Современный и отзывчивый интерфейс

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

Откройте браузер и перейдите по адресу, который покажет Vite (обычно http://localhost:5173)

## Синтаксис языка Карамба

### Переменные
```
пусть имя = значение;
```

### Условия
```
если (условие) то {
    // код
} иначе {
    // код
}
```

### Циклы
```
пока (условие) {
    // код
}
```

### Функции
```
функция имя(параметр1, параметр2) {
    // код
    вернуть значение;
}
```

### Вывод
```
вывести значение;
```

### Операторы
- Арифметические: `+`, `-`, `*`, `/`, `%`
- Сравнения: `==`, `!=`, `<`, `>`, `<=`, `>=`
- Логические: `и`, `или`, `не`

## Примеры

Смотрите примеры кода прямо в редакторе - нажмите кнопку "Пример"!

## Технологии

- TypeScript
- React
- Vite
- Собственный компилятор (лексер, парсер, интерпретатор)

